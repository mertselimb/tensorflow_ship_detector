The model,images,and training logs isn't included for many reasons.
Training log dir is training.
Model dir is ship_detector_graph
Image dir is image/train  , image/test

The dataset is from kaggle airbus ship detection challange
data dir is for .csv and .record data

.config file will be included in the main dir but it should be in training directory.

Used model is faster_rcnn_resnet50_coco_2018_01_28 and can be downloaded from tensorflow models